export enum Status {
    OPEN = 'open',
    CLOSED = 'closed',
}

export enum MessageType {
    PLATFORM = 'from_platform',
    MANAGEMENT = 'from_management',
}

export enum ElementType {
    CATEGORY = 'category',
    ITEM = 'item',
}
